"""
pinterest/ — Pinterest automation package.

Modules:
    playwright_publisher  ← main entry point
    session_manager       ← login + session persistence
    board_manager         ← board cache + content rules
    image_downloader      ← download with retry
    pin_creator           ← upload + fill + publish
    pinterest_selectors   ← centralized DOM selectors
    retry_utils           ← retry helpers
"""
