# ═══════════════════════════════════════════════════════════════════════════
# requirements_phase1.txt — Phase 1: Core Infrastructure
# ═══════════════════════════════════════════════════════════════════════════
#
# Install: pip install -r requirements_phase1.txt
#
# These requirements ADD TO the existing project requirements.
# Do NOT remove the existing requirements.txt — keep both.
# During migration, both systems run in parallel.
# ═══════════════════════════════════════════════════════════════════════════

# ── Configuration ──────────────────────────────────────────────────────────
pydantic>=2.7.0                      # Data validation (v2 API)
pydantic-settings>=2.3.0             # Settings from env vars (v2)

# ── Database (async PostgreSQL) ────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.29          # ORM with async support
asyncpg>=0.29.0                      # Async PostgreSQL driver (fastest)
psycopg2-binary>=2.9.9               # Sync driver for Alembic migrations only
alembic>=1.13.1                      # Database migration tool

# ── Redis & Queue ──────────────────────────────────────────────────────────
redis[asyncio]>=5.0.4                # Redis client with asyncio support
celery[redis]>=5.4.0                 # Distributed task queue
flower>=2.0.1                        # Celery monitoring UI (optional)

# ── Async HTTP (scraping foundation — Phase 2) ────────────────────────────
httpx[http2]>=0.27.0                 # Async HTTP client with HTTP/2
aiohttp>=3.9.5                       # Alternative async HTTP client

# ── Web Framework (Phase 6 — FastAPI migration) ───────────────────────────
fastapi>=0.111.0                     # Async web framework
uvicorn[standard]>=0.29.0            # ASGI server for FastAPI

# ── Existing dependencies (keep — do not remove) ──────────────────────────
pyTelegramBotAPI>=4.20.0
flask>=3.0.0
requests>=2.32.0
beautifulsoup4>=4.12.3
curl-cffi>=0.7.3
lxml
gspread>=6.1.2
oauth2client>=4.1.3
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2
google-api-python-client>=2.131.0
Pillow>=10.3.0
playwright>=1.44.0
playwright-stealth>=1.0.6
python-dotenv>=1.0.1
urllib3>=2.2.1
qrcode[pil]>=7.4.2
cloudscraper>=1.2.71
